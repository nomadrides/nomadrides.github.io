- if data.rides.current
  div class="p-home-current-ride" id="current-ride"
    div class="ride"
      h2
        | We're currently in
        strong< = data.rides.current.location
        | !
      div class="ride-details"
        p class="sith ride-period"
          = data.rides.current.period
        p class="maul"
          | 🙌 🙌 🙌
      = link_to "Let's meet!", protect_email(data.site.mail), class: "button ghost"
